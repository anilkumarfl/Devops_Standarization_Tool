# CLI framework and prompt handling
typer>=0.9.0

# Parsing the infra.yaml and rules engine files
PyYAML>=6.0

# Templating engine for generating Terraform files without hardcoding
Jinja2>=3.1.2

# ── AI providers (install whichever you use) ──────────────────────────────────

# Claude (Anthropic) — set ANTHROPIC_API_KEY
# AI_PROVIDER=claude  AI_MODEL=claude-sonnet-4-6
anthropic>=0.40.0

# OpenAI — set OPENAI_API_KEY
# AI_PROVIDER=openai  AI_MODEL=gpt-4o
openai>=1.0.0

# Gemini (Google) — set GOOGLE_API_KEY
# AI_PROVIDER=gemini  AI_MODEL=gemini-2.0-flash
google-generativeai>=0.8.0

# Kimi / Moonshot AI — set MOONSHOT_API_KEY
# AI_PROVIDER=kimi  AI_MODEL=kimi-k2.7-code
# Uses OpenAI-compatible API (openai package above covers this — no extra install needed)
# Base URL: https://api.moonshot.cn/v1
